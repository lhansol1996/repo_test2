<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.infra.code.CodeDao">

	<resultMap id="resultMapObj" type="com.example.demo.infra.code.CodeDto"></resultMap>

    <select id="testList" resultMap="resultMapObj">
        select 
		a.codeGroup_seq as codeGroupSeq
		,b.name as codeGroupName
		,a.delNy 
		,a.seq
		,a.name
		,a.modifyDate 
		From code a join codeGroup b 
		on a.codegroup_seq = b.seq	        
    </select>
	  
</mapper>